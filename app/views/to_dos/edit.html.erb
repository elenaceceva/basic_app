<%= link_to "Back", new_to_do_path %>
<%= form_for (@to_do) do |f| %>
  <p>
    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.label "Mark for complete" %>
    <%= f.check_box :status %>
    <%= f.submit "Save changes" %>
<% end %>
<p>Task status: </p>
<%= @to_do.status %>
